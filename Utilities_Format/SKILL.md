---
name: Utilities_Format
description: 负责标准化的格式塔转换、长文档提炼、网页数据抓取与提取。
---

# 🚀 全能格式转换与数据工厂 (Utilities Format)

> **SYSTEM INSTRUCTION**: 这是一级路由目录 (Dispatcher)。此处不包含具体的执行逻辑。

## 🎯 模块定位 (Purpose)
负责标准化的格式塔转换、长文档提炼、网页数据抓取与提取。

## 📂 子技能路由表 (Sub-skills)
当前目录下包含多个专属的子模组 (Sub-directories)。

**包含的技能清单**：
* **[document-summarizer]**: 批量总结 PDF/DOCX/PPTX/XLSX 文件，附带智能中文摘要和标签体系。
* **[docx]**: 创建、读取、编辑或操作 Word 文档（.docx 文件）。生成带格式排版的高级专业文档。
* **[excel-pro]**: 重装级 Excel 治理系统。整合物理层 XML 操纵与逻辑层数据建模，实现从数据摄取到专业模型固化的全栈闭环。
* **[markdown-converter]**: 将任何文档、图像或媒体文件转换为干净的 Markdown 原质。由 Microsoft MarkItDown 强力驱动。
* **[pdf]**: 读取、提取、合并、拆分、旋转、加水印、创建、表单填写、加密以及对 PDF 进行底层 OCR 处理。
* **[pptx]**: 全面接管演示文稿：创建幻灯片、提取文本、编辑汇报内容、合并/拆分以及模板与母版操控。
* **[tuanbiaodownloader]**: 从团体标准存容库批量下载影像，并全自动合并装订为高保真 PDF 案卷。
* **[url-to-markdown]**: 抓取任何 URL（包括重度 JS 或登录隔离网页）并转换为干净的 markdown。直控 Chrome CDP 协议。


> [!IMPORTANT]
> **🚀 双重确认挂载协议 (Two-Pass Capability Selection)**
>
> 你严禁单纯依据上方极其简短的描述就直接“盲猜”并全量使用某个子技能进行大规模生产！
> 
> **当你锁定了一个或多个潜在子技能时，你必须（MUST）：**
> 1. **第一阶段 (Pass 1 - 候选集圈定)**：选取 1~3 个听起来最相关的子技能。
> 2. **第二阶段 (Pass 2 - 深度校验)**：使用 `view_file` 或读取命令，**强制去读取**对应子文件夹下具体的 `SKILL.md`，查看其内部的 "USE WHEN" 和 "执行法则"。
> 3. **第三阶段 (Commit - 执行)**：对比完毕后，再从中挑选真正最符合用户意图的最佳技能，启动正式的生产步骤。
>
> 违背此流程将导致严重的战略失误。

---
*SYS_CHECK: Utilities_Format Router Loaded. Awaiting drill-down instruction.*
