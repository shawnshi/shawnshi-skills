\documentclass[twocolumn, 10pt, a4paper]{ctexart}

% ---------------------------------------------------------
% 1. Layout & Fonts
% ---------------------------------------------------------
\usepackage{geometry}
\geometry{
    top=2cm, 
    bottom=2cm, 
    left=1.5cm, 
    right=1.5cm
}
\usepackage{amsmath, amssymb, amsfonts}
\usepackage{newtxtext, newtxmath} % Standard academic fonts (Times-like)
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{url}
\usepackage{longtable}
\usepackage{tabularx}
\usepackage{array}
\usepackage{calc}
\usepackage{abstract}
\usepackage{hyperref}

% ---------------------------------------------------------
% Fixes for Pandoc
% ---------------------------------------------------------
\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}
$if(highlighting-macros)$
$highlighting-macros$
$endif$
$if(tables)$
\usepackage{longtable,booktabs,array}
$endif$

% ---------------------------------------------------------
% 2. Bibliography (Chinese Standard)
% ---------------------------------------------------------
\usepackage[super,square,sort&compress]{gbt7714} 

% ---------------------------------------------------------
% 3. Styling
% ---------------------------------------------------------
\usepackage{titlesec}
\titleformat{\section}{\large\bfseries}{\thesection}{1em}{}
\titlespacing*{\section}{0pt}{10pt}{5pt}

$if(title)$
\title{\textbf{\Large $title$}}
$endif$

$if(author)$
\author{\large $author$}
$endif$

$if(date)$
\date{$date$}
$else$
\date{\today}
$endif$

\begin{document}

% Special handling for single-column abstract in two-column doc
$if(title)$
\twocolumn[
  \begin{@twocolumnfalse}
    \maketitle
$if(abstract)$
    \renewcommand{\abstractname}{\large 摘要}
    \begin{abstract}
      \normalsize
      $abstract$
    \end{abstract}
$endif$
    \vspace{1.5cm}
  \end{@twocolumnfalse}
]
$endif$

$body$

% Bibliography placeholder (uncomment if using BibTeX)
% \bibliographystyle{gbt7714-numerical}
% \bibliography{ref}

\end{document}