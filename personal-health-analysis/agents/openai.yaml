interface:
  display_name: "Personal Health Analysis"
  short_description: "显式调用默认读取全部 Garmin 健康指标，本地优先"
  default_prompt: "Use $personal-health-analysis to analyze all supported Garmin health metrics for my requested period, default to the latest seven days, prefer verified local data, and ask separately before network or other side effects."
policy:
  allow_implicit_invocation: false
