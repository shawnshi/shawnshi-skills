\documentclass[11pt, a4paper]{article}

% ---------------------------------------------------------
% 1. Layout & Packages
% ---------------------------------------------------------
% 2.6cm side margins keep the measure at ~90 characters per line at 11pt;
% 1cm margins produced ~108.  Page numbers stay on: CVs are often 2+ pages.
\usepackage[top=1.6cm, bottom=1.6cm, left=2.6cm, right=2.6cm]{geometry}
\usepackage{graphicx}
\usepackage{titlesec}
\usepackage{enumitem}
\usepackage[hidelinks]{hyperref}
\usepackage[$if(cjk)$scheme=chinese,$else$scheme=plain,$endif$]{ctex}
\usepackage{xcolor}
\usepackage{fontawesome5} % Icons
\usepackage{paracol}      % Two-column layout
\usepackage{longtable}
\usepackage{booktabs}
\usepackage{tabularx}
\usepackage{array}
\usepackage{calc}
$if(cjk)$$else$
\usepackage[document]{ragged2e} % English body text is left-aligned
$endif$

% ---------------------------------------------------------
% 2. Language routing & Pandoc compatibility
% ---------------------------------------------------------
$if(linespread)$
\linespread{$linespread$}\selectfont
$endif$
\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}
\providecommand{\passthrough}[1]{#1}
% Mirrors the parts of Pandoc's own LaTeX prelude these presets need.
% Pandoc >= 3 wraps images in \pandocbounded so they fit the text block.
\makeatletter
\@ifundefined{pandoc@box}{\newsavebox\pandoc@box}{}
\providecommand*\pandocbounded[1]{%
  \sbox\pandoc@box{#1}%
  \Gscale@div\@tempa{\textheight}{\dimexpr\ht\pandoc@box+\dp\pandoc@box\relax}%
  \Gscale@div\@tempb{\linewidth}{\wd\pandoc@box}%
  \ifdim\@tempb\p@<\@tempa\p@\let\@tempa\@tempb\fi
  \ifdim\@tempa\p@<\p@\scalebox{\@tempa}{\usebox\pandoc@box}%
  \else\usebox{\pandoc@box}%
  \fi}
\def\fps@figure{htbp}
% --citeproc citation commands and reference list
\@ifundefined{citeproc}{%
  \NewDocumentCommand\citeproctext{}{}
  \NewDocumentCommand\citeproc{mm}{%
    \begingroup\def\citeproctext{#2}\cite{#1}\endgroup}
  \let\@cite@ofmt\@firstofone
  \def\@biblabel#1{}
  \def\@cite#1#2{{#1\if@tempswa , #2\fi}}}{}
\@ifundefined{CSLReferences}{%
  \newlength{\cslhangindent}
  \setlength{\cslhangindent}{1.5em}
  \newlength{\csllabelwidth}
  \setlength{\csllabelwidth}{3em}
  \newenvironment{CSLReferences}[2]{%
    \begin{list}{}{%
      \setlength{\itemindent}{0pt}%
      \setlength{\leftmargin}{0pt}%
      \setlength{\parsep}{0pt}%
      \ifodd #1
        \setlength{\leftmargin}{\cslhangindent}%
        \setlength{\itemindent}{-1\cslhangindent}%
      \fi
      \setlength{\itemsep}{#2\baselineskip}}}{%
    \end{list}}
  \newcommand{\CSLBlock}[1]{\hfill\break\parbox[t]{\linewidth}{\strut\ignorespaces#1\strut}}
  \newcommand{\CSLLeftMargin}[1]{\parbox[t]{\csllabelwidth}{\strut#1\strut}}
  \newcommand{\CSLRightInline}[1]{\parbox[t]{\linewidth - \csllabelwidth}{\strut#1\strut}}
  \newcommand{\CSLIndent}[1]{\hspace{\cslhangindent}#1}}{}
\makeatother
\setlength{\emergencystretch}{3em} % prevent overfull lines
\makeatletter
\@ifundefined{c@none}{\newcounter{none}}{}
\makeatother
$if(numbersections)$
\setcounter{secnumdepth}{$if(secnumdepth)$$secnumdepth$$else$3$endif$}
$else$
\setcounter{secnumdepth}{-\maxdimen}
$endif$
$if(highlighting-macros)$
$highlighting-macros$
$endif$

% ---------------------------------------------------------
% 3. Styling
% ---------------------------------------------------------
\pagestyle{plain}
\definecolor{primaryColor}{RGB}{50, 50, 50}
\definecolor{lineColor}{RGB}{200, 200, 200}

% Custom section styling with underline
\titleformat{\section}
  {\Large\bfseries\raggedright\color{primaryColor}}
  {}
  {0em}
  {}[\color{lineColor}\titlerule]
\titlespacing{\section}{0pt}{10pt}{5pt}

% Compact lists
\setlist[itemize]{leftmargin=*, noitemsep, topsep=0pt}

\begin{document}

% ---------------------------------------------------------
% Header
% ---------------------------------------------------------
$if(title)$
\begin{center}
    {\Huge \textbf{$title$}} \\ \vspace{5pt} 
$if(author)$
    $author$
$endif$
    % Example contact line (template placeholders)
    % \faEnvelope\ email@example.com | \faPhone\ +86 123-4567-8900 | \faGithub\ github.com/user
    \\ \vspace{5pt} 
$if(date)$
    $date$
$else$
    \today
$endif$
\end{center}
\vspace{0.5cm}
$endif$

% ---------------------------------------------------------
% Content (Supports paracol for split layout)
% ---------------------------------------------------------
% Usage example for user content:
% \columnratio{0.65} % 65% Main content, 35% Sidebar
% \begin{paracol}{2}
% Left Column (Experience)
% ...
% \switchcolumn
% Right Column (Skills)
% ...
% \end{paracol}

$body$

\end{document}
