\documentclass[$if(cjk)$scheme=chinese,$else$scheme=plain,$endif$twocolumn, 10pt, a4paper]{ctexart}

% ---------------------------------------------------------
% 1. Layout & Fonts
% ---------------------------------------------------------
\usepackage{geometry}
\geometry{
    top=2cm, 
    bottom=2cm, 
    left=1.5cm, 
    right=1.5cm
}
\usepackage{amsmath, amssymb, amsfonts}
\usepackage{newtxtext, newtxmath} % Standard academic fonts (Times-like)
% newtxtext pushes "Extension = .otf" into fontspec's default features.  xeCJK
% resolves its CJK families lazily, so the leaked extension turns every CJK
% monospace request (any inline code, any fenced code block) into a fatal
% 'The font "FangSong" cannot be found'.  Clearing the defaults restores normal
% resolution for every font loaded afterwards.
\defaultfontfeatures{}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{url}
\usepackage{longtable}
\usepackage{tabularx}
\usepackage{array}
\usepackage{calc}
\usepackage{abstract}
$if(cjk)$$else$
\usepackage[document]{ragged2e} % English body text is left-aligned
$endif$

% ---------------------------------------------------------
% 2. Bibliography (Chinese Standard)
% ---------------------------------------------------------
\usepackage[super,square,sort&compress]{gbt7714} 

% ---------------------------------------------------------
% 3. Styling
% ---------------------------------------------------------
\usepackage{titlesec}
\titleformat{\section}{\large\bfseries}{\thesection}{1em}{}
\titlespacing*{\section}{0pt}{10pt}{5pt}

% ---------------------------------------------------------
% 4. Pandoc compatibility
% ---------------------------------------------------------
\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}
\providecommand{\passthrough}[1]{#1}
% Mirrors the parts of Pandoc's own LaTeX prelude these presets need.
% Pandoc >= 3 wraps images in \pandocbounded so they fit the text block.
\makeatletter
\@ifundefined{pandoc@box}{\newsavebox\pandoc@box}{}
\providecommand*\pandocbounded[1]{%
  \sbox\pandoc@box{#1}%
  \Gscale@div\@tempa{\textheight}{\dimexpr\ht\pandoc@box+\dp\pandoc@box\relax}%
  \Gscale@div\@tempb{\linewidth}{\wd\pandoc@box}%
  \ifdim\@tempb\p@<\@tempa\p@\let\@tempa\@tempb\fi
  \ifdim\@tempa\p@<\p@\scalebox{\@tempa}{\usebox\pandoc@box}%
  \else\usebox{\pandoc@box}%
  \fi}
\def\fps@figure{htbp}
% --citeproc citation commands and reference list
\@ifundefined{citeproc}{%
  \NewDocumentCommand\citeproctext{}{}
  \NewDocumentCommand\citeproc{mm}{%
    \begingroup\def\citeproctext{#2}\cite{#1}\endgroup}
  \let\@cite@ofmt\@firstofone
  \def\@biblabel#1{}
  \def\@cite#1#2{{#1\if@tempswa , #2\fi}}}{}
\@ifundefined{CSLReferences}{%
  \newlength{\cslhangindent}
  \setlength{\cslhangindent}{1.5em}
  \newlength{\csllabelwidth}
  \setlength{\csllabelwidth}{3em}
  \newenvironment{CSLReferences}[2]{%
    \begin{list}{}{%
      \setlength{\itemindent}{0pt}%
      \setlength{\leftmargin}{0pt}%
      \setlength{\parsep}{0pt}%
      \ifodd #1
        \setlength{\leftmargin}{\cslhangindent}%
        \setlength{\itemindent}{-1\cslhangindent}%
      \fi
      \setlength{\itemsep}{#2\baselineskip}}}{%
    \end{list}}
  \newcommand{\CSLBlock}[1]{\hfill\break\parbox[t]{\linewidth}{\strut\ignorespaces#1\strut}}
  \newcommand{\CSLLeftMargin}[1]{\parbox[t]{\csllabelwidth}{\strut#1\strut}}
  \newcommand{\CSLRightInline}[1]{\parbox[t]{\linewidth - \csllabelwidth}{\strut#1\strut}}
  \newcommand{\CSLIndent}[1]{\hspace{\cslhangindent}#1}}{}
\makeatother
\setlength{\emergencystretch}{3em} % prevent overfull lines
\makeatletter
\@ifundefined{c@none}{\newcounter{none}}{}
\makeatother
$if(numbersections)$
\setcounter{secnumdepth}{$if(secnumdepth)$$secnumdepth$$else$3$endif$}
$else$
\setcounter{secnumdepth}{-\maxdimen}
$endif$
$if(linespread)$
\linespread{$linespread$}\selectfont
$endif$
$if(highlighting-macros)$
$highlighting-macros$
$endif$

% ---------------------------------------------------------
% 5. Hyperref (loaded last, no red link boxes)
% ---------------------------------------------------------
\usepackage[hidelinks, colorlinks=true, linkcolor=black, urlcolor=black, citecolor=black]{hyperref}

$if(title)$
\title{\textbf{\Large $title$}}
$endif$

$if(author)$
\author{\large $author$}
$endif$

$if(date)$
\date{$date$}
$else$
\date{\today}
$endif$

\begin{document}

% Special handling for single-column abstract in two-column doc
$if(title)$
\twocolumn[
  \begin{@twocolumnfalse}
    \maketitle
$if(abstract)$
    \renewcommand{\abstractname}{\large $if(cjk)$摘要$else$Abstract$endif$}
    \begin{abstract}
      \normalsize
      $abstract$
    \end{abstract}
$endif$
    \vspace{1.5cm}
  \end{@twocolumnfalse}
]
$elseif(abstract)$
% An abstract does not require title metadata or a title block.
\twocolumn[
  \begin{@twocolumnfalse}
    \renewcommand{\abstractname}{\large $if(cjk)$摘要$else$Abstract$endif$}
    \begin{abstract}
      \normalsize
      $abstract$
    \end{abstract}
    \vspace{1.5cm}
  \end{@twocolumnfalse}
]
$endif$

$body$

% Bibliography placeholder (uncomment if using BibTeX)
% \bibliographystyle{gbt7714-numerical}
% \bibliography{ref}

\end{document}
